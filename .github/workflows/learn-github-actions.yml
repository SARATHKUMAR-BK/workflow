name: learn-github-actions
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Person to greet'
        required: true
        default: 'sarath'
      home:
        description: 'location'
        required: false
        default: 'Gobi'
jobs:
  call_another:
      runs-on: self-hosted
      steps:
       - uses: actions/checkout@v2
         with:
            path: boc_github_workflows_test/actions
       - name: Call s3 cration file
         if: github.event.inputs.name == 'sarath'
         uses: ./boc_github_workflows_test/actions/new/actions
         with: 
            name: ${{ github.event.inputs.name }}
            location: ${{ github.event.inputs.home }}
