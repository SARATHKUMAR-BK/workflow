name: 's3 creation'
inputs:
  name:
    default: sarath
    required: true
  location:
    default: Gobi
    required: true
runs:
  using: composite
  steps:
    - name: Name
      run: echo "${{ inputs.name }}"
      shell: bash
    - name: Location
      run: |
        echo "${{ inputs.location }}"
      shell: bash
